services:
  app:
    build: .
    ports:
      - "5001:5000"
    env_file: .env
    volumes:
      - sqlite_data:/app/data

  client:
    build: ./client
    ports:
      - "3000:3000"
    depends_on:
      - app

volumes:
  sqlite_data:
